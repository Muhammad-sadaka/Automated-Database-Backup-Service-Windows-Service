# Automated-Database-Backup-Service-Windows-Service
A lightweight, background Windows Service developed in C# and .NET Framework to automate full SQL Server database backups at customizable intervals. It features automated logging, fallback directory options, and dual-mode execution (interactive Console debugging and background Windows Service mode).
